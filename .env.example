# Upstream service URLs
DATA_API_BASE=https://propiq-data-api-429012647952.me-central1.run.app
CHARTS_API_BASE=https://propiq-charts-api-429012647952.me-central1.run.app
# charts-img is PARKED (ADR 0017 — static PNG rendering moved to the
# consumer/NAR side). Leave empty: the chart client refuses loudly rather
# than call a dead host, and /health no longer gates on it. Set a URL here
# only to re-enable (the park is config-reversible). See issue #17.
CHARTS_IMG_BASE=

# LLM — Vertex AI primary (uses GCP ADC, no key needed)
ANTHROPIC_MODEL=claude-sonnet-4-6
VERTEX_PROJECT=crowdproperty-440707
VERTEX_REGION=us-east5

# LLM — Direct Anthropic API fallback (optional, enables fallback on 429/rate limits)
# On Cloud Run: mounted from Secret Manager (anthropic-api-key)
# Locally: set here or leave empty to use Vertex AI only
ANTHROPIC_API_KEY=

# Generation auth (see app/auth.py and issue #7).
# On Cloud Run: mounted from Secret Manager (see operator sibling).
# Locally: leave empty — endpoints stay open for dev convenience.
CONTENT_ENGINE_API_KEY=

# Output
OUTPUT_DIR=./output

# GCP (for BigQuery scout_news access)
GCP_PROJECT=crowdproperty-440707
